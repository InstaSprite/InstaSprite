package com.instasprite.app.utils

import com.instasprite.app.BuildConfig

object Constants {
    const val BASE_URL = BuildConfig.BASE_URL
    const val IMG_URL = BuildConfig.IMG_URL

    // 1 image px = PIXEL_DP_RATIO.dp
    const val PIXEL_DP = 1.5
}